var nums = [2,3,0,1,7,5]

function findMin(arr){
    var min = arr[0]
    const len = arr.length
    for (i=1, i<len; i++){
        if (arr[i]< min){
            min = arr[i]
        }
    }
    return min
}

functionMinIndex (arr)}{
    
}